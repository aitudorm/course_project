import { SafeResourceUrl } from "@angular/platform-browser";

export class cartProduct {
  cartId: string;
  id: number;
  name: string;
  type: string;
  price: string;
  gender: string;
  image: SafeResourceUrl;
}
